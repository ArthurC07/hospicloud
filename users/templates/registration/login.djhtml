<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  {% load static %}
  {% get_static_prefix as STATIC_PREFIX %} <script type="text/javascript" src="{{ STATIC_PREFIX }}js/modernizr.js" ></script>
  <link rel="stylesheet" href="{{ STATIC_PREFIX }}css/reset.css" />
  <link rel="stylesheet" href="{{ STATIC_PREFIX }}css/grid.css" />
  <link rel="stylesheet" href="{{ STATIC_PREFIX }}css/login.css" />
  <title>HospiNet :: Iniciar Sesi&oacute;n</title>
</head>
<body>
  {% if form.errors %}
  <p>
    Su nombre de usuario y contrase&ntilde;a no coinciden. Por favor intente
    otra vez.
  </p>
  {% endif %}
  <div id="contenedor" class="container_12 content clearfix">
    <form method="post" action="{% url login %}">
      <hgroup>
        <h2>Iniciar Sesi&oacute;n</h2>
      </hgroup>
      <fieldset id="user-details">
        {% csrf_token %}
        {{ form.username.label_tag }}
        {{ form.username }}
        <br />
        {{ form.password.label_tag }}
        {{ form.password }}
        <input type="submit" value="Iniciar Sesión" />
        <input type="hidden" name="next" value="{{ next }}" />
      </fieldset>
    </form>
  </div>
</body>
</html>
